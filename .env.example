# OpenRouter API Key - REQUIRED
# Get your key at https://openrouter.ai/keys
OPENROUTER_KEY=your_openrouter_key_here

# Reddit API Credentials - RECOMMENDED for AWS/cloud deployments
# Create these at https://www.reddit.com/prefs/apps by clicking "create app" at the bottom
# Choose "script" as the type
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=python:idea-filter:v1.0 (by /u/yourusername)

# Optional: HTTP Proxy for bypassing IP blocks
# HTTP_PROXY=http://your-proxy-service:port

# Email Configuration - OPTIONAL (but required if you want email reports)
# Recipient email address (where reports will be sent)
RECIPIENT_EMAIL=your_email@example.com

# SMTP Settings (for Gmail, Yahoo, etc.)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Your email credentials
# For Gmail, you need to use an App Password: https://myaccount.google.com/apppasswords
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here

# Sender email (defaults to SMTP_USERNAME if not specified)
SENDER_EMAIL=your_email@gmail.com 